---
phase: 07-ui-improvements
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - MathGaze/Styles/AppStyles.xaml
  - MathGaze/Styles/Themes/Light.xaml
  - MathGaze/Styles/Themes/Dark.xaml
  - MathGaze/App.xaml
  - MathGaze/App.xaml.cs
  - MathGaze/ViewModels/SettingsViewModel.cs
  - MathGaze/Properties/Settings.settings
  - MathGaze/Views/TopBar.xaml
  - MathGaze/MainWindow.xaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can click the gear button in the top bar to open an in-window settings panel"
    - "The settings panel contains a Light / Dark toggle with 56x56px targets"
    - "Toggling Dark mode changes all WPF shell colours immediately without restart"
    - "The selected theme persists across app restarts"
    - "The settings panel closes via a close button; the close button is >=56x56px"
    - "No secondary HWND, Popup, or flyout is created (Grid 3 compatible)"
  artifacts:
    - path: "MathGaze/Styles/Themes/Light.xaml"
      provides: "Light theme colour brushes"
      contains: "BrushBg"
    - path: "MathGaze/Styles/Themes/Dark.xaml"
      provides: "Dark theme colour brushes"
      contains: "BrushBg"
    - path: "MathGaze/ViewModels/SettingsViewModel.cs"
      provides: "IsDarkMode, IsSettingsPanelOpen, toggle commands"
      exports: ["SettingsViewModel"]
    - path: "MathGaze/Properties/Settings.settings"
      provides: "Theme preference persistence"
      contains: "Theme"
  key_links:
    - from: "TopBar.xaml gear button"
      to: "SettingsViewModel.OpenSettingsCommand"
      via: "DataContext binding to MainViewModel.SettingsVm"
      pattern: "OpenSettingsCommand"
    - from: "App.xaml.cs ApplyTheme()"
      to: "Application.Current.Resources.MergedDictionaries[0]"
      via: "ResourceDictionary swap at index 0"
      pattern: "MergedDictionaries\\[0\\]"
    - from: "SettingsViewModel.SetThemeCommand"
      to: "App.ApplyTheme() + Properties.Settings.Default.Save()"
      via: "((App)Application.Current).ApplyTheme(isDark)"
      pattern: "ApplyTheme"
---

<objective>
Implement the ResourceDictionary-based theme system (Light/Dark) and wire the gear button to an in-window settings panel.

Purpose: The student's daily experience is materially affected by screen glare in exam rooms. Dark mode reduces glare with a single-toggle switch in a Grid 3 compatible panel (no flyouts, no secondary windows). Theme preference persists across restarts.

Output: Light.xaml and Dark.xaml theme files; App.xaml restructured to load theme at index 0; SettingsViewModel with toggle command and Properties.Settings persistence; in-window settings panel in MainWindow.xaml; gear button wired to open it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-ui-improvements/07-CONTEXT.md
@.planning/phases/07-ui-improvements/07-RESEARCH.md
@.planning/phases/07-ui-improvements/07-01-SUMMARY.md

<interfaces>
<!-- Key types and patterns from the live codebase. -->

From MathGaze/App.xaml (current — after Plan 07-01):
```xml
<!-- Current single merged dictionary -->
<ResourceDictionary Source="Styles/AppStyles.xaml"/>
```

From MathGaze/App.xaml.cs (current DI registrations):
```csharp
services.AddSingleton<MainViewModel>();
services.AddSingleton<ToolViewModel>();
services.AddSingleton<RightRailViewModel>();
// SettingsViewModel is NOT yet registered — add it here
```

From MathGaze/ViewModels/MainViewModel.cs:
```csharp
public partial class MainViewModel : ObservableObject
{
    // Add: public SettingsViewModel SettingsVm { get; }
    // The gear button in TopBar binds via DataContext=MainViewModel
}
```

From MathGaze/Styles/AppStyles.xaml (brush keys to extract to theme files):
All 11 SolidColorBrush definitions will move to Light.xaml and Dark.xaml:
  BrushBg, BrushSurface, BrushSurface2, BrushBorder,
  BrushInk, BrushInk2, BrushInk3,
  BrushAccent, BrushAccentSoft, BrushAccentInk, BrushTransparent

After the move, AppStyles.xaml will contain ONLY:
  FontFamily declarations, ToolTileStyle, IconButtonStyle, RailButtonStyle,
  StepButtonStyle, DeleteButtonStyle

CommunityToolkit.Mvvm patterns (established):
```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;

public partial class SettingsViewModel : ObservableObject
{
    [ObservableProperty] private bool _isDarkMode;
    [ObservableProperty] private bool _isSettingsPanelOpen;
    [RelayCommand] private void OpenSettings()  => IsSettingsPanelOpen = true;
    [RelayCommand] private void CloseSettings() => IsSettingsPanelOpen = false;
    [RelayCommand] private void SetTheme(string theme) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract brush keys into Light.xaml and Dark.xaml; restructure App.xaml</name>
  <files>
    MathGaze/Styles/Themes/Light.xaml,
    MathGaze/Styles/Themes/Dark.xaml,
    MathGaze/Styles/AppStyles.xaml,
    MathGaze/App.xaml,
    MathGaze/Properties/Settings.settings,
    MathGaze/App.xaml.cs
  </files>
  <read_first>
    - MathGaze/Styles/AppStyles.xaml (identify the 11 brush definitions to extract)
    - MathGaze/App.xaml (current MergedDictionaries structure)
    - MathGaze/App.xaml.cs (DI registrations and startup code)
  </read_first>
  <action>
**Step 1 — Create MathGaze/Styles/Themes/ directory and Light.xaml:**

Create `MathGaze/Styles/Themes/Light.xaml` with exactly these 11 brush key definitions (light values):

```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="BrushBg"          Color="#F5F3EE"/>
    <SolidColorBrush x:Key="BrushSurface"     Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BrushSurface2"    Color="#FAF8F3"/>
    <SolidColorBrush x:Key="BrushBorder"      Color="#E8E6E0"/>
    <SolidColorBrush x:Key="BrushInk"         Color="#1A1D24"/>
    <SolidColorBrush x:Key="BrushInk2"        Color="#4A4E58"/>
    <SolidColorBrush x:Key="BrushInk3"        Color="#8A8E98"/>
    <SolidColorBrush x:Key="BrushAccent"      Color="#3B6FD4"/>
    <SolidColorBrush x:Key="BrushAccentSoft"  Color="#EEF2FB"/>
    <SolidColorBrush x:Key="BrushAccentInk"   Color="#2A4FA0"/>
    <SolidColorBrush x:Key="BrushTransparent" Color="Transparent"/>
</ResourceDictionary>
```

**Step 2 — Create MathGaze/Styles/Themes/Dark.xaml:**

Create `MathGaze/Styles/Themes/Dark.xaml` with the dark equivalent values (cobalt accent unchanged per D-07):

```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="BrushBg"          Color="#1A1C22"/>
    <SolidColorBrush x:Key="BrushSurface"     Color="#22252E"/>
    <SolidColorBrush x:Key="BrushSurface2"    Color="#1E2028"/>
    <SolidColorBrush x:Key="BrushBorder"      Color="#3A3D47"/>
    <SolidColorBrush x:Key="BrushInk"         Color="#F0EEE8"/>
    <SolidColorBrush x:Key="BrushInk2"        Color="#A8ACBA"/>
    <SolidColorBrush x:Key="BrushInk3"        Color="#6A6E7A"/>
    <SolidColorBrush x:Key="BrushAccent"      Color="#3B6FD4"/>
    <SolidColorBrush x:Key="BrushAccentSoft"  Color="#1E2A45"/>
    <SolidColorBrush x:Key="BrushAccentInk"   Color="#7BA4F0"/>
    <SolidColorBrush x:Key="BrushTransparent" Color="Transparent"/>
</ResourceDictionary>
```

**Step 3 — Remove brush keys from AppStyles.xaml:**

Delete the 11 `<SolidColorBrush ...>` lines from AppStyles.xaml (the "Colour brushes" block at the top). Leave everything else: FontFamily declarations and the 5 button styles. AppStyles.xaml now depends on the theme dictionary being loaded first in the merge order.

**Step 4 — Restructure App.xaml:**

Replace the current `<ResourceDictionary.MergedDictionaries>` block so it has two merged dictionaries (theme at index 0, styles at index 1):

```xml
<Application.Resources>
    <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <!-- Index 0: theme dictionary — swapped at runtime by App.ApplyTheme() -->
            <ResourceDictionary Source="Styles/Themes/Light.xaml"/>
            <!-- Index 1: non-theme styles (button templates, typography) -->
            <ResourceDictionary Source="Styles/AppStyles.xaml"/>
        </ResourceDictionary.MergedDictionaries>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="BoolToInverseVisibilityConverter"/>
    </ResourceDictionary>
</Application.Resources>
```

The `xmlns:converters` declaration must already be on the `<Application>` element (it is — read the file to confirm before editing).

**Step 5 — Create Properties/Settings.settings:**

Create the file `MathGaze/Properties/Settings.settings` with content:

```xml
<?xml version='1.0' encoding='utf-8'?>
<SettingsFile xmlns="http://schemas.microsoft.com/VisualStudio/2004/01/settings" CurrentProfile="(Default)" GeneratedClassNamespace="MathGaze.Properties" GeneratedClassName="Settings">
  <Profiles/>
  <Settings>
    <Setting Name="Theme" Type="System.String" Scope="User">
      <Value Profile="(Default)">Light</Value>
    </Setting>
  </Settings>
</SettingsFile>
```

**Step 6 — Add ApplyTheme() to App.xaml.cs:**

In `App.xaml.cs`, add the following public method to the `App` class:

```csharp
/// <summary>
/// Swaps the theme ResourceDictionary at MergedDictionaries[0].
/// Called by SettingsViewModel on toggle and on startup.
/// </summary>
public void ApplyTheme(bool isDark)
{
    var dict = new ResourceDictionary
    {
        Source = new Uri(
            isDark ? "Styles/Themes/Dark.xaml" : "Styles/Themes/Light.xaml",
            UriKind.Relative)
    };
    Current.Resources.MergedDictionaries[0] = dict;
}
```

Add the following at the TOP of the `OnStartup` method body, BEFORE `_host = Host.CreateDefaultBuilder()...`, so the saved theme is applied before the window is shown:

```csharp
// Apply saved theme preference before building the window
Properties.Settings.Default.Upgrade();  // migrate from previous version if any
bool savedDark = Properties.Settings.Default.Theme == "Dark";
if (savedDark) ApplyTheme(true);  // Light is the default — no call needed unless Dark
```

**Step 7 — Register SettingsViewModel in DI:**

In `App.xaml.cs`, inside the `ConfigureServices` lambda, add after the existing ViewModel registrations:

```csharp
services.AddSingleton<SettingsViewModel>();
```
  </action>
  <verify>
    Check: File `MathGaze/Styles/Themes/Light.xaml` exists and contains `BrushBg` with `Color="#F5F3EE"`.
    Check: File `MathGaze/Styles/Themes/Dark.xaml` exists and contains `BrushBg` with `Color="#1A1C22"`.
    Check: AppStyles.xaml does NOT contain `SolidColorBrush` definitions (grep should return 0).
    Check: App.xaml contains `Source="Styles/Themes/Light.xaml"` as the first MergedDictionary.
    Check: App.xaml.cs contains `ApplyTheme`.
    Build: `dotnet build MathGaze/MathGaze.csproj 2>&1 | tail -10` — exits 0.
  </verify>
  <acceptance_criteria>
    - `MathGaze/Styles/Themes/Light.xaml` exists with all 11 brush keys including `BrushBg Color="#F5F3EE"`
    - `MathGaze/Styles/Themes/Dark.xaml` exists with all 11 brush keys including `BrushBg Color="#1A1C22"`
    - `MathGaze/Styles/AppStyles.xaml` contains 0 occurrences of `SolidColorBrush`
    - `MathGaze/App.xaml` first MergedDictionary Source is `Styles/Themes/Light.xaml`
    - `MathGaze/App.xaml.cs` contains method `ApplyTheme(bool isDark)`
    - `MathGaze/App.xaml.cs` contains `Properties.Settings.Default.Upgrade()`
    - `MathGaze/App.xaml.cs` DI registrations include `AddSingleton<SettingsViewModel>()`
    - `MathGaze/Properties/Settings.settings` exists and contains `Name="Theme"`
    - `dotnet build MathGaze/MathGaze.csproj` exits with 0 errors
  </acceptance_criteria>
  <done>Theme dictionaries created and loaded; AppStyles.xaml contains only styles (no brushes); App.xaml.cs applies saved theme on startup and exposes ApplyTheme(); Settings.settings persists the Theme preference; SettingsViewModel registered in DI.</done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsViewModel, expose via MainViewModel, and wire settings panel in MainWindow.xaml and TopBar.xaml</name>
  <files>
    MathGaze/ViewModels/SettingsViewModel.cs,
    MathGaze/ViewModels/MainViewModel.cs,
    MathGaze/Views/TopBar.xaml,
    MathGaze/MainWindow.xaml
  </files>
  <read_first>
    - MathGaze/ViewModels/MainViewModel.cs (constructor, existing properties — need to add SettingsVm property)
    - MathGaze/Views/TopBar.xaml (gear button location and current state after Task 1 sizing)
    - MathGaze/MainWindow.xaml (current Grid structure for overlay placement)
  </read_first>
  <action>
**Step 1 — Create MathGaze/ViewModels/SettingsViewModel.cs:**

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using System.Windows;

namespace MathGaze.ViewModels;

/// <summary>
/// Drives the in-window settings panel (D-04, D-05, D-06, D-07).
/// IsDarkMode state is the source of truth; ApplyTheme delegates to App.
/// </summary>
public partial class SettingsViewModel : ObservableObject
{
    [ObservableProperty] private bool _isDarkMode;
    [ObservableProperty] private bool _isSettingsPanelOpen;

    public SettingsViewModel()
    {
        // Initialise from the persisted preference so the toggle shows the correct state
        _isDarkMode = Properties.Settings.Default.Theme == "Dark";
    }

    [RelayCommand]
    private void OpenSettings() => IsSettingsPanelOpen = true;

    [RelayCommand]
    private void CloseSettings() => IsSettingsPanelOpen = false;

    /// <summary>
    /// Called from the Light/Dark toggle buttons in the settings panel.
    /// Parameter: "Light" or "Dark".
    /// </summary>
    [RelayCommand]
    private void SetTheme(string theme)
    {
        bool isDark = theme == "Dark";
        IsDarkMode = isDark;
        ((App)Application.Current).ApplyTheme(isDark);
        Properties.Settings.Default.Theme = theme;
        Properties.Settings.Default.Save();
    }
}
```

**Step 2 — Add SettingsVm property to MainViewModel:**

In `MathGaze/ViewModels/MainViewModel.cs`:

Add `SettingsViewModel` to the constructor parameter list and store it:

```csharp
private readonly SettingsViewModel _settingsVm;

// In constructor:
public MainViewModel(
    IPdfService pdfService,
    IFileDialogService fileDialogService,
    IGeometryService geometryService,
    ISessionService sessionService,
    IExportService exportService,
    ToolViewModel toolViewModel,
    SettingsViewModel settingsViewModel)   // ADD THIS PARAMETER
{
    _pdfService        = pdfService;
    _fileDialogService = fileDialogService;
    _geometryService   = geometryService;
    _sessionService    = sessionService;
    _exportService     = exportService;
    _toolVm            = toolViewModel;
    _settingsVm        = settingsViewModel;  // ADD THIS LINE
}

/// <summary>Exposed for TopBar and MainWindow DataContext binding.</summary>
public SettingsViewModel SettingsVm => _settingsVm;
```

**Step 3 — Wire gear button in TopBar.xaml:**

The gear button in TopBar.xaml currently has no Command. Add the command binding. The TopBar DataContext is MainViewModel (set in MainWindow.xaml), so the path is `SettingsVm.OpenSettingsCommand`:

Find the Settings gear button (currently `DockPanel.Dock="Right"`, Width="56" Height="56" after Task 1). Add:

```xml
Command="{Binding SettingsVm.OpenSettingsCommand}"
ToolTip="Settings"
```

Remove the old `ToolTip="Settings (not yet implemented)"` and replace with `ToolTip="Settings"`.

**Step 4 — Add in-window settings panel overlay to MainWindow.xaml:**

The settings panel is an overlay `Border` inside the main window's content grid, positioned over the right rail area. It uses `Panel.ZIndex` to float above the right rail.

In `MainWindow.xaml`, inside the content `Grid` (Grid.Row="1"), add the settings panel as the LAST child (so it draws on top via natural z-order, or set Panel.ZIndex="10"):

```xml
<!-- Settings panel overlay — covers right rail area (D-04) -->
<!-- Width matches the right rail (148px). Z-order above right rail. -->
<!-- Grid 3 compatible: in-window element, no secondary HWND, no Popup. -->
<Border x:Name="SettingsPanelOverlay"
        Grid.Column="2"
        Panel.ZIndex="10"
        Background="{DynamicResource BrushSurface}"
        BorderBrush="{DynamicResource BrushBorder}"
        BorderThickness="1,0,0,0"
        Visibility="{Binding SettingsVm.IsSettingsPanelOpen, Converter={StaticResource BoolToVisibilityConverter}}">
    <DockPanel Margin="8,14,8,0">

        <!-- Header: SETTINGS label + close button -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
                    HorizontalAlignment="Stretch" Margin="0,0,0,12">
            <TextBlock Text="SETTINGS"
                       FontSize="9" FontWeight="Bold"
                       Foreground="{DynamicResource BrushInk3}"
                       VerticalAlignment="Center"
                       Margin="0,0,0,0"
                       HorizontalAlignment="Left"/>
            <Button Width="56" Height="56"
                    Style="{StaticResource RailButtonStyle}"
                    Command="{Binding SettingsVm.CloseSettingsCommand}"
                    Content="&#x2715;"
                    FontSize="16"
                    HorizontalAlignment="Right"
                    Margin="4,0,0,0"
                    ToolTip="Close settings"/>
        </StackPanel>

        <!-- Theme section -->
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="THEME"
                       FontSize="9" FontWeight="Bold"
                       Foreground="{DynamicResource BrushInk3}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>
            <!-- Light / Dark toggle — StepButtonStyle with Tag="active" for active state -->
            <UniformGrid Rows="1" Columns="2" Margin="0,0,0,6">
                <Button Height="56"
                        Command="{Binding SettingsVm.SetThemeCommand}"
                        CommandParameter="Light"
                        Content="Light"
                        ToolTip="Switch to Light theme">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource StepButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SettingsVm.IsDarkMode}" Value="False">
                                    <Setter Property="Tag" Value="active"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Height="56"
                        Command="{Binding SettingsVm.SetThemeCommand}"
                        CommandParameter="Dark"
                        Content="Dark"
                        ToolTip="Switch to Dark theme">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource StepButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SettingsVm.IsDarkMode}" Value="True">
                                    <Setter Property="Tag" Value="active"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </UniformGrid>
        </StackPanel>

    </DockPanel>
</Border>
```

Note on HEADER layout: the `StackPanel Orientation="Horizontal"` with `HorizontalAlignment="Stretch"` needs the TextBlock to fill remaining space and the button to be right-aligned. Use a `DockPanel` inside instead:

```xml
<DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
    <Button DockPanel.Dock="Right"
            Width="56" Height="56"
            Style="{StaticResource RailButtonStyle}"
            Command="{Binding SettingsVm.CloseSettingsCommand}"
            Content="&#x2715;"
            FontSize="16"
            ToolTip="Close settings"/>
    <TextBlock Text="SETTINGS"
               FontSize="9" FontWeight="Bold"
               Foreground="{DynamicResource BrushInk3}"
               VerticalAlignment="Center"/>
</DockPanel>
```

The settings panel DataContext is inherited from the window (which is MainViewModel). The `SettingsVm.XxxCommand` path works because MainViewModel exposes `SettingsVm` as a public property.
  </action>
  <verify>
    Check: `MathGaze/ViewModels/SettingsViewModel.cs` exists and contains `SetThemeCommand`.
    Check: `MathGaze/ViewModels/MainViewModel.cs` contains `public SettingsViewModel SettingsVm`.
    Check: `MathGaze/Views/TopBar.xaml` gear button contains `Command="{Binding SettingsVm.OpenSettingsCommand}"`.
    Check: `MathGaze/MainWindow.xaml` contains `SettingsPanelOverlay` and `IsSettingsPanelOpen`.
    Build: `dotnet build MathGaze/MathGaze.csproj 2>&1 | tail -10` — exits 0.
  </verify>
  <acceptance_criteria>
    - `MathGaze/ViewModels/SettingsViewModel.cs` exists with `[RelayCommand] private void SetTheme(string theme)` and `Properties.Settings.Default.Save()`
    - `MathGaze/ViewModels/MainViewModel.cs` has `SettingsViewModel` constructor parameter and `public SettingsViewModel SettingsVm => _settingsVm`
    - `MathGaze/Views/TopBar.xaml` gear button has `Command="{Binding SettingsVm.OpenSettingsCommand}"`
    - `MathGaze/MainWindow.xaml` has `Border x:Name="SettingsPanelOverlay"` with `Visibility` bound to `SettingsVm.IsSettingsPanelOpen`
    - `MathGaze/MainWindow.xaml` settings panel has Light and Dark `Button` elements each with `Height="56"` and `CommandParameter` of "Light" / "Dark"
    - `dotnet build MathGaze/MathGaze.csproj` exits with 0 errors
  </acceptance_criteria>
  <done>SettingsViewModel created; MainViewModel exposes SettingsVm; gear button opens settings panel; settings panel closes via X button; Light/Dark toggle swaps theme immediately and persists preference; no secondary HWNDs used.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Properties.Settings → disk | Theme preference ("Light"/"Dark") written to user.config in %APPDATA% |
| User input → SetTheme command | Theme toggle accepts a CommandParameter string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | Properties.Settings user.config | accept | File stores only "Light" or "Dark". Even if tampered, `ApplyTheme(isDark)` treats unknown values as Light (the `== "Dark"` check means anything other than "Dark" defaults to Light). No code execution from this value. |
| T-07-02-02 | Tampering | SetThemeCommand CommandParameter | accept | Parameter is bound from XAML to hardcoded string literals "Light" / "Dark". No user-typed input reaches this command. The command ignores unrecognised values (only `== "Dark"` check used). |
| T-07-02-03 | Denial of Service | ResourceDictionary swap | accept | Swap replaces in-memory object; failure would throw a WPF exception visible in debug mode only. Not a security concern for a desktop exam tool. |

No high-severity threats. All data flows are low-sensitivity UI preferences.
</threat_model>

<verification>
After both tasks complete:
1. `dotnet build MathGaze/MathGaze.csproj` passes with 0 errors.
2. Launch the app. Click the gear button — settings panel slides in over the right rail with SETTINGS header, close button (X), THEME label, Light/Dark buttons.
3. Click Dark — the entire UI shell immediately changes to dark colours. Light button highlights cobalt, Dark is now cobalt-active.
4. Close and relaunch the app — dark theme is applied on startup without clicking anything.
5. Click gear again, click Light — shell returns to light colours. Relaunch confirms Light persists.
6. Confirm: no popup windows, no system dialogs appear during theme toggle.
</verification>

<success_criteria>
- Theme dictionaries (Light.xaml, Dark.xaml) exist with all 11 brush keys
- AppStyles.xaml contains 0 SolidColorBrush definitions (brushes moved to theme files)
- App.xaml MergedDictionaries[0] is the theme file (swapped at runtime)
- Gear button opens in-window settings panel (no popup, no secondary HWND)
- Light/Dark toggle works immediately; preference survives app restart
- Build passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-improvements/07-02-SUMMARY.md` with:
- Files created (Light.xaml, Dark.xaml, SettingsViewModel.cs, Settings.settings)
- Files modified (AppStyles.xaml, App.xaml, App.xaml.cs, MainViewModel.cs, TopBar.xaml, MainWindow.xaml)
- Build status
- Any WPF DynamicResource issues encountered and resolved
</output>
