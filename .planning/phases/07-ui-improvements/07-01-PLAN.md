---
phase: 07-ui-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MathGaze/Views/TopBar.xaml
  - MathGaze/Views/ScrollRail.xaml
  - MathGaze/Views/ToolRail.xaml
  - MathGaze/Styles/AppStyles.xaml
  - MathGaze/MainWindow.xaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "All top-bar buttons (Open, Close, Zoom-/+/Fit, Prev/Next page, Settings gear) are at least 56x56px clickable targets"
    - "Tool rail buttons are 84x84px square with icon above label; Protractor label reads 'Protractor' on one line"
    - "Scroll rail buttons are 56x56px; rail width is 64px"
    - "Top bar height is 72px"
    - "All brush key references in button templates use DynamicResource for theme-swappable keys"
  artifacts:
    - path: "MathGaze/Views/TopBar.xaml"
      provides: "Updated top bar with 56x56 buttons and 72px height"
      contains: "Height=\"72\""
    - path: "MathGaze/Views/ScrollRail.xaml"
      provides: "Updated scroll rail with 56x56 buttons and 64px width"
      contains: "Width=\"64\""
    - path: "MathGaze/Views/ToolRail.xaml"
      provides: "Updated tool rail with 84x84 vertical-stack buttons"
      contains: "Orientation=\"Vertical\""
    - path: "MathGaze/Styles/AppStyles.xaml"
      provides: "Updated ToolTileStyle (84x84) and DynamicResource brush refs in templates"
      contains: "Height\"            Value=\"84\""
  key_links:
    - from: "MathGaze/MainWindow.xaml"
      to: "RowDefinition Height=72"
      via: "TopBar height update"
      pattern: "Height=\"72\""
---

<objective>
Resize all gaze-interactive buttons to meet the >=56x56px gaze floor and redesign the tool rail to 84x84px square icon-above-label layout.

Purpose: All interactive elements in the app shell must meet the gaze accuracy floor so the student can reliably activate them without a mouse. This phase also converts StaticResource brush references in button templates to DynamicResource, which is required for the theme system built in Plan 07-02.

Output: TopBar, ToolRail, and ScrollRail with correct dimensions. AppStyles.xaml with DynamicResource brush references inside ControlTemplates. MainWindow.xaml row height updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ui-improvements/07-CONTEXT.md
@.planning/phases/07-ui-improvements/07-RESEARCH.md

<interfaces>
<!-- Key XAML dimensions and styles the executor needs. Extracted from live codebase. -->

From MathGaze/Styles/AppStyles.xaml (current state before this plan):
- ToolTileStyle: Width=84, Height=56, ContentPresenter HorizontalAlignment="Left", VerticalAlignment="Center", Margin="12,0,0,0"
- IconButtonStyle: MinWidth=36, MinHeight=36, Width=36, Height=36
- RailButtonStyle: uses StaticResource BrushSurface2, BrushAccentSoft, BrushAccent in ControlTemplate triggers
- StepButtonStyle: uses StaticResource BrushAccent, BrushAccentInk, BrushSurface2, BrushAccentSoft in ControlTemplate triggers

From MathGaze/Views/TopBar.xaml (current state):
- UserControl Height="60"
- OpenButton: Width="36" Height="36"
- CloseButton: Width="36" Height="36"
- Settings gear button: Width="40" Height="40", DockPanel.Dock="Right"
- Zoom out/in/fit buttons: Width="32" Height="32"
- PrevPage button: Width="36" Height="32"
- NextPage button: Width="36" Height="32"
- PDF Export button: Width="56" Height="56" (already correct — no change)

From MathGaze/Views/ToolRail.xaml (current state):
- UserControl Width="104"
- All buttons: Style="{StaticResource ToolTileStyle}" with StackPanel Orientation="Horizontal"
  - Viewbox Width="24" Height="24" Margin="0,0,10,0"
  - TextBlock VerticalAlignment="Center"
  - Protractor button TextBlock has FontSize="11" set inline

From MathGaze/Views/ScrollRail.xaml (current state):
- UserControl Width="38"
- All 4 buttons: Width="30" Height="56"

From MathGaze/MainWindow.xaml (current state):
- RowDefinition Height="60" (TopBar row)
- ColumnDefinition Width="104" (ToolRail column)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resize TopBar buttons to 56x56px and ScrollRail to 56x56px / 64px wide</name>
  <files>MathGaze/Views/TopBar.xaml, MathGaze/Views/ScrollRail.xaml, MathGaze/MainWindow.xaml</files>
  <read_first>
    - MathGaze/Views/TopBar.xaml (current sizes and structure)
    - MathGaze/Views/ScrollRail.xaml (current sizes and structure)
    - MathGaze/MainWindow.xaml (RowDefinition and ColumnDefinition widths)
  </read_first>
  <action>
**TopBar.xaml changes:**

1. Change `UserControl Height="60"` to `Height="72"` (per D-12 — 56px button + 8px top/bottom padding).

2. Set `OpenButton` to `Width="56" Height="56"` (currently 36x36). Element-level width/height overrides style setters — same pattern as Delete button.

3. Set `CloseButton` to `Width="56" Height="56"` (currently 36x36).

4. Set the Settings gear `Button` (currently Width="40" Height="40", DockPanel.Dock="Right") to `Width="56" Height="56"`. Keep DockPanel.Dock="Right". Note: D-04 requires this button to open the settings panel (command wiring is done in Plan 07-02).

5. In the Zoom strip: set Zoom-out, Zoom-in, and Fit-page buttons to `Width="56" Height="56"` (currently 32x32). The surrounding `Border` uses `Margin="3"` for padding — keep that.

6. In the Page navigation strip: set Prev-page and Next-page buttons to `Width="56" Height="56"` (currently Width="36" Height="32"). The surrounding `Border` uses `Margin="3"` for padding — keep that.

7. The PDF Export button is already `Width="56" Height="56"` — no change.

8. The page counter TextBlock and zoom label TextBlock are not buttons — no dimension change to those (per D-13).

**ScrollRail.xaml changes:**

1. Change `UserControl Width="38"` to `Width="64"` (per D-17).

2. Set all four scroll buttons to `Width="56"` (currently 30). Height stays 56 — no change. Affects: PageUp button, ScrollUp button, ScrollDown button, PageDown button.

**MainWindow.xaml changes:**

1. Change the TopBar RowDefinition from `Height="60"` to `Height="72"` to match the new TopBar height.

2. Change the ToolRail ColumnDefinition from `Width="104"` to `Width="108"` (per D-15 — 84px button + ~12px padding each side = 108px; must match ToolRail UserControl Width update in Task 2).
  </action>
  <verify>
    Check: TopBar.xaml contains `Height="72"` on the UserControl element.
    Check: TopBar.xaml contains `Width="56" Height="56"` on OpenButton, CloseButton, and settings gear button.
    Check: TopBar.xaml contains `Width="56" Height="56"` on zoom-out, zoom-in, fit-page, prev-page, next-page buttons.
    Check: ScrollRail.xaml contains `Width="64"` on the UserControl.
    Check: ScrollRail.xaml contains `Width="56"` on all four scroll buttons (grep for `Width="56"` — should appear 4 times in button elements).
    Check: MainWindow.xaml contains `Height="72"` in the first RowDefinition.
  </verify>
  <acceptance_criteria>
    - TopBar.xaml: `UserControl` has `Height="72"`
    - TopBar.xaml: `OpenButton` has `Width="56" Height="56"`
    - TopBar.xaml: `CloseButton` has `Width="56" Height="56"`
    - TopBar.xaml: Settings gear Button has `Width="56" Height="56"`
    - TopBar.xaml: Zoom-out Button has `Width="56" Height="56"`
    - TopBar.xaml: Zoom-in Button has `Width="56" Height="56"`
    - TopBar.xaml: Fit-page Button has `Width="56" Height="56"`
    - TopBar.xaml: Prev-page Button has `Width="56" Height="56"`
    - TopBar.xaml: Next-page Button has `Width="56" Height="56"`
    - ScrollRail.xaml: `UserControl` has `Width="64"`
    - ScrollRail.xaml: All 4 buttons have `Width="56"`
    - MainWindow.xaml: First RowDefinition has `Height="72"`
  </acceptance_criteria>
  <done>All top-bar interactive buttons are 56x56px; scroll rail is 56x56px buttons in 64px-wide rail; top bar height 72px; MainWindow row updated to match.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ToolTileStyle and ToolRail to 84x84px icon-above-label; convert button template brush refs to DynamicResource</name>
  <files>MathGaze/Styles/AppStyles.xaml, MathGaze/Views/ToolRail.xaml</files>
  <read_first>
    - MathGaze/Styles/AppStyles.xaml (current ToolTileStyle, RailButtonStyle, StepButtonStyle, DeleteButtonStyle, IconButtonStyle templates)
    - MathGaze/Views/ToolRail.xaml (current button content structure)
  </read_first>
  <action>
**AppStyles.xaml changes — ToolTileStyle:**

1. Change `Height` setter from `Value="56"` to `Value="84"` (per D-14 — square 84x84).

2. In the ControlTemplate's `ContentPresenter`, change:
   - `HorizontalAlignment="Left"` to `HorizontalAlignment="Center"`
   - `VerticalAlignment="Center"` to `VerticalAlignment="Center"` (already correct)
   - `Margin="12,0,0,0"` to `Margin="0"` (icon-above-label layout needs no left margin — content is centred)

3. In the ToolTileStyle ControlTemplate triggers, change `StaticResource` to `DynamicResource` for all theming brush keys:
   - `{StaticResource BrushSurface2}` → `{DynamicResource BrushSurface2}` (IsMouseOver hover)
   - `{StaticResource BrushAccentSoft}` → `{DynamicResource BrushAccentSoft}` (IsPressed)
   - `{StaticResource BrushAccent}` → `{DynamicResource BrushAccent}` (IsPressed BorderBrush)

**AppStyles.xaml changes — RailButtonStyle ControlTemplate triggers:**

4. Change `StaticResource` to `DynamicResource` for all theming brush keys in the RailButtonStyle ControlTemplate:
   - IsMouseOver: `{StaticResource BrushSurface2}` → `{DynamicResource BrushSurface2}`
   - IsPressed Background: `{StaticResource BrushAccentSoft}` → `{DynamicResource BrushAccentSoft}`
   - IsPressed BorderBrush: `{StaticResource BrushAccent}` → `{DynamicResource BrushAccent}`

Also update the style-level Setters (not just triggers) for RailButtonStyle:
   - `Value="{StaticResource BrushSurface}"` (Background) → `{DynamicResource BrushSurface}`
   - `Value="{StaticResource BrushBorder}"` (BorderBrush) → `{DynamicResource BrushBorder}`
   - `Value="{StaticResource BrushInk}"` (Foreground) → `{DynamicResource BrushInk}`

**AppStyles.xaml changes — StepButtonStyle ControlTemplate triggers:**

5. Change `StaticResource` to `DynamicResource` for all theming brush keys in StepButtonStyle:
   - Style-level setters: BrushSurface (Background), BrushBorder (BorderBrush), BrushInk (Foreground)
   - Tag=active trigger: BrushAccent (Background + BorderBrush)
   - IsMouseOver: BrushSurface2
   - IsMouseOver+Tag=active: BrushAccentInk
   - IsPressed: BrushAccentSoft

**AppStyles.xaml changes — IconButtonStyle ControlTemplate triggers:**

6. Change `StaticResource` to `DynamicResource` for theming brush keys in IconButtonStyle:
   - Style-level setters: BrushBorder (BorderBrush), BrushInk2 (Foreground)
   - IsMouseOver: BrushSurface2
   - IsPressed: BrushAccentSoft

**AppStyles.xaml changes — DeleteButtonStyle:**

7. DeleteButtonStyle uses hardcoded hex colours in triggers (`#CC2020`, `#991818`, `#660000`) — those are not brush keys and stay unchanged. But update the style-level setters inherited from RailButtonStyle that reference brush keys (those were already updated in step 4).

**NOTE:** Do NOT change the style-level Setters on `ToolTileStyle` for Background, BorderBrush, Foreground from StaticResource to DynamicResource — those are applied once at element creation and do not need to be dynamic because ToolRail.xaml DataTriggers set Background/BorderBrush directly as active-state overrides. The active-highlight DataTriggers in ToolRail.xaml also use StaticResource — change those to DynamicResource as well (see ToolRail.xaml changes below).

**ToolRail.xaml changes:**

8. Change `UserControl Width="104"` to `Width="108"` (per D-15: 84px button + 12px padding each side).

9. For each of the 6 tool buttons, change the `StackPanel Orientation="Horizontal"` to `Orientation="Vertical"`:
   - Inside each StackPanel: remove `Margin="0,0,10,0"` from the Viewbox (no horizontal gap needed in vertical layout)
   - Add `HorizontalAlignment="Center"` to the Viewbox
   - Change `VerticalAlignment="Center"` on the TextBlock to `HorizontalAlignment="Center"`
   - Remove `VerticalAlignment="Center"` from the TextBlock (not applicable in vertical stack)

10. Remove the `FontSize="11"` override on the Protractor button TextBlock — at 84px width, "Protractor" fits at the default FontSize="12" from ToolTileStyle (per D-14).

11. In each tool button's inline Style triggers (the DataTrigger for `ActiveTool`), change:
    - `Value="{StaticResource BrushAccentSoft}"` → `Value="{DynamicResource BrushAccentSoft}"`
    - `Value="{StaticResource BrushAccent}"` → `Value="{DynamicResource BrushAccent}"`

12. In the "TOOLS" header TextBlock, change:
    - `Foreground="{StaticResource BrushAccentInk}"` → `Foreground="{DynamicResource BrushAccentInk}"`

Also update remaining StaticResource brush references in ToolRail.xaml (Path Fill, Ellipse Fill, Path Stroke, Ellipse Stroke) within the icon Canvas elements — each uses `{StaticResource BrushInk}`. Change these to `{DynamicResource BrushInk}`.

Also update StaticResource brush refs in TopBar.xaml, ScrollRail.xaml, and RightRail.xaml for all BrushXxx keys that need to respond to theme swap. The rule: any `{StaticResource BrushBg}`, `{StaticResource BrushSurface}`, `{StaticResource BrushSurface2}`, `{StaticResource BrushBorder}`, `{StaticResource BrushInk}`, `{StaticResource BrushInk2}`, `{StaticResource BrushInk3}`, `{StaticResource BrushAccentInk}`, `{StaticResource BrushAccentSoft}` should become DynamicResource everywhere in XAML files (not just templates). BrushAccent and BrushTransparent are identical in both themes and can stay StaticResource.

Do a thorough replacement across TopBar.xaml, ScrollRail.xaml, ToolRail.xaml, RightRail.xaml (but NOT AppStyles.xaml style-level setters that use StaticResource for non-templated property chains). For clarity: in XAML views, change all `{StaticResource BrushSurface}`, `{StaticResource BrushSurface2}`, `{StaticResource BrushBorder}`, `{StaticResource BrushInk}`, `{StaticResource BrushInk2}`, `{StaticResource BrushInk3}`, `{StaticResource BrushAccentInk}`, `{StaticResource BrushAccentSoft}`, `{StaticResource BrushBg}` to DynamicResource.

Also update the MainWindow.xaml `Background="{StaticResource BrushBg}"` → `{DynamicResource BrushBg}` and the Background attributes on ToolRail.xaml and RightRail.xaml.
  </action>
  <verify>
    Build: `cd "C:/Local Docs/Coding/MathGaze" && dotnet build MathGaze/MathGaze.csproj --no-restore 2>&1 | tail -5`
    Check: AppStyles.xaml ToolTileStyle contains `Height"            Value="84"`
    Check: AppStyles.xaml has no `{StaticResource BrushSurface2}` inside ControlTemplate triggers (all should be DynamicResource)
    Check: ToolRail.xaml contains `Orientation="Vertical"` inside tool button StackPanels
    Check: ToolRail.xaml contains `Width="108"` on UserControl
    Check: Protractor TextBlock in ToolRail.xaml does NOT contain `FontSize="11"`
  </verify>
  <acceptance_criteria>
    - AppStyles.xaml: ToolTileStyle Height setter is `Value="84"`
    - AppStyles.xaml: ToolTileStyle ContentPresenter has `HorizontalAlignment="Center"` and no left margin
    - AppStyles.xaml: All ControlTemplate trigger Setters referencing BrushSurface2, BrushAccentSoft, BrushAccent, BrushAccentInk, BrushSurface, BrushBorder, BrushInk use `DynamicResource` (not StaticResource)
    - ToolRail.xaml: UserControl Width is `108`
    - ToolRail.xaml: All 6 tool button StackPanels have `Orientation="Vertical"`
    - ToolRail.xaml: Protractor TextBlock has no `FontSize="11"` override
    - `dotnet build MathGaze/MathGaze.csproj` exits with 0 errors
  </acceptance_criteria>
  <done>ToolRail buttons are 84x84px square with icon centred above label; "Protractor" fits without truncation; all brush keys inside ControlTemplates and view XAML files use DynamicResource for theme-swappable colours; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None significant | All changes are pure XAML dimension and style changes. No data crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | AppStyles.xaml | accept | XAML is compiled into the application binary at build time; no runtime load from untrusted path. Style changes cannot be injected at runtime. |

No high-severity threats identified. All changes are purely visual dimension and style edits with no data input, no file I/O, and no trust boundaries crossed.
</threat_model>

<verification>
After both tasks complete:
1. `dotnet build MathGaze/MathGaze.csproj` passes with 0 errors.
2. Open the app and visually confirm: TopBar button row is visibly taller; all buttons are clearly square and large. ToolRail buttons show icon above label. ScrollRail is wider with square buttons.
3. Grep: `grep -n "StaticResource BrushSurface2\|StaticResource BrushAccentSoft\|StaticResource BrushSurface\b\|StaticResource BrushBorder\|StaticResource BrushInk[^k]" MathGaze/Styles/AppStyles.xaml` — should return 0 matches inside `<ControlTemplate.Triggers>` sections (those must all be DynamicResource).
</verification>

<success_criteria>
- TopBar height is 72px; all 8 interactive buttons are 56x56px (PDF Export was already correct)
- ToolRail is 108px wide; all 6 tool buttons are 84x84px square with icon above label; "Protractor" label shows on one line
- ScrollRail is 64px wide; all 4 scroll buttons are 56x56px
- Build passes with 0 errors
- All BrushXxx keys used inside ControlTemplate triggers use DynamicResource (required for Plan 07-02 theme swap to work)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-improvements/07-01-SUMMARY.md` with:
- What was changed (files, specific dimension changes)
- Any deviations from the plan and why
- Build status
- DynamicResource conversion scope (which files were updated)
</output>
