---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - MathGaze/MainWindow.xaml
  - MathGaze/MainWindow.xaml.cs
  - MathGaze/App.xaml
  - MathGaze/App.xaml.cs
  - MathGaze/ViewModels/MainViewModel.cs
  - MathGaze/Views/TopBar.xaml
  - MathGaze/Views/TopBar.xaml.cs
  - MathGaze/Views/ToolRail.xaml
  - MathGaze/Views/ToolRail.xaml.cs
  - MathGaze/Views/RightRailPlaceholder.xaml
  - MathGaze/Views/RightRailPlaceholder.xaml.cs
  - MathGaze/Views/PdfCanvas.xaml
  - MathGaze/Views/PdfCanvas.xaml.cs
  - MathGaze/Views/ScrollRail.xaml
  - MathGaze/Views/ScrollRail.xaml.cs
  - MathGaze/Styles/AppStyles.xaml
autonomous: false
requirements:
  - CORE-03

must_haves:
  truths:
    - "App launches to a 3-column window: left tool rail, centre canvas, right rail"
    - "TopBar is visible across the full width above all columns"
    - "Left tool rail shows 6 stub buttons: Select, Point, Line, Circle, Protractor, Text — each at least 84×56 WPF logical units"
    - "Mode pill in TopBar shows Practice Mode or Exam Mode with a coloured dot"
    - "Right rail shows a dashed 'Nothing selected' placeholder"
    - "Window title reads MathGaze"
  artifacts:
    - path: "MathGaze/ViewModels/MainViewModel.cs"
      provides: "Top-level ViewModel: FileName, IsPracticeMode observable properties"
      exports: ["MainViewModel"]
    - path: "MathGaze/Views/TopBar.xaml"
      provides: "TopBar UserControl — branding, file chip, mode pill, zoom strip, page nav strip"
    - path: "MathGaze/Views/ToolRail.xaml"
      provides: "Left rail with 6 ToolTile stub buttons (no commands wired)"
    - path: "MathGaze/Views/RightRailPlaceholder.xaml"
      provides: "Empty state right rail (dashed border, Nothing selected text)"
    - path: "MathGaze/Styles/AppStyles.xaml"
      provides: "ToolTileStyle ResourceDictionary merged in App.xaml"
  key_links:
    - from: "MathGaze/MainWindow.xaml"
      to: "MathGaze/ViewModels/MainViewModel.cs"
      via: "DataContext binding"
      pattern: "DataContext"
    - from: "MathGaze/Views/TopBar.xaml"
      to: "MathGaze/ViewModels/MainViewModel.cs"
      via: "Binding IsPracticeMode, FileName"
      pattern: "Binding IsPracticeMode"
---

<objective>
Build the complete 3-column WPF shell: TopBar UserControl (visual + bindings), ToolRail with 6 gaze-sized stub buttons, SKXamlCanvas placeholder in the centre, ScrollRail overlay, and RightRail empty state. MainViewModel provides the observable state these views bind to.

Purpose: After Plan 01 the project compiles but shows only a scaffold TextBlock. This plan delivers the complete visual frame that Plans 03 and 04 fill with live behaviour. The shell must match the direction-splitrails.jsx design reference exactly.

Output: A running WPF window that looks correct but has no live PDF data yet. All gaze targets are >= 56px. No flyouts, no popups, all UI in-window panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@docs/direction-splitrails.jsx
@docs/shared.jsx
@docs/HANDOFF.md

<interfaces>
<!-- Produced by Plan 01 — executor uses these directly -->

From MathGaze/Core/CoordinateMapper.cs:
```csharp
public sealed class CoordinateMapper
{
    public CoordinateMapper(double zoomFactor, double dpiScale, double pageWidthPt, double pageHeightPt, double canvasOriginX = 0, double canvasOriginY = 0);
    public SKPoint PageToScreen(double xPt, double yPt);
    public (double xPt, double yPt) ScreenToPage(SKPoint screenPx);
    public SKRect GetPageDestRect(int canvasWidthPx, int canvasHeightPx);
    public double PageWidthPx { get; }
    public double PageHeightPx { get; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens, AppStyles, and MainViewModel</name>
  <files>
    MathGaze/Styles/AppStyles.xaml
    MathGaze/App.xaml
    MathGaze/App.xaml.cs
    MathGaze/ViewModels/MainViewModel.cs
  </files>
  <read_first>
    - docs/shared.jsx (design tokens: T.bg, T.surface, T.surface2, T.border, T.accent, T.ink, T.ink2, T.ink3, T.s(n) scale function at comfortable density = 1:1)
    - docs/direction-splitrails.jsx (TopBar, ToolRail layout — font, sizes, colours)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: MVVM observable properties)
    - MathGaze/App.xaml.cs (current DI wiring to extend with MainViewModel registration)
  </read_first>
  <action>
Design token mapping from shared.jsx buildTokens({ theme: 'light', accent: 'cobalt', density: 'comfortable' }):
- bg:       #f5f3ee
- surface:  #ffffff
- surface2: #faf8f3
- border:   rgba(20,20,25,0.10) → use #E8E6E0 (pre-multiplied approximation for WPF solid brushes)
- ink:      #1a1d24
- ink2:     #4a4e58
- ink3:     #8a8e98
- accent:   oklch(0.58 0.17 245) → cobalt ≈ #3B6FD4 (use this hex)
- accentSoft: oklch(0.94 0.05 245) → ≈ #EEF2FB
- accentInk: oklch(0.42 0.14 245) → ≈ #2A4FA0
- exam color: oklch(0.56 0.16 25) → ≈ #D94F2A (orange-red)
- practice color: oklch(0.56 0.14 160) → ≈ #2CA870 (green)
- font: Inter (system fallback: Segoe UI)
- mono: JetBrains Mono (system fallback: Consolas)
- T.s(n) at comfortable density = n (scale 1.0)

1. Create MathGaze/Styles/ directory. Create MathGaze/Styles/AppStyles.xaml:
```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ── Colour brushes (design tokens) ── -->
    <SolidColorBrush x:Key="BrushBg"          Color="#F5F3EE"/>
    <SolidColorBrush x:Key="BrushSurface"     Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BrushSurface2"    Color="#FAF8F3"/>
    <SolidColorBrush x:Key="BrushBorder"      Color="#E8E6E0"/>
    <SolidColorBrush x:Key="BrushInk"         Color="#1A1D24"/>
    <SolidColorBrush x:Key="BrushInk2"        Color="#4A4E58"/>
    <SolidColorBrush x:Key="BrushInk3"        Color="#8A8E98"/>
    <SolidColorBrush x:Key="BrushAccent"      Color="#3B6FD4"/>
    <SolidColorBrush x:Key="BrushAccentSoft"  Color="#EEF2FB"/>
    <SolidColorBrush x:Key="BrushAccentInk"   Color="#2A4FA0"/>
    <SolidColorBrush x:Key="BrushExam"        Color="#D94F2A"/>
    <SolidColorBrush x:Key="BrushPractice"    Color="#2CA870"/>
    <SolidColorBrush x:Key="BrushTransparent" Color="Transparent"/>

    <!-- ── Typography ── -->
    <FontFamily x:Key="FontBody">Inter, Segoe UI, sans-serif</FontFamily>
    <FontFamily x:Key="FontMono">JetBrains Mono, Consolas, monospace</FontFamily>

    <!-- ── ToolTile button style (gaze target: 84×56 logical px minimum) ── -->
    <!-- Used by ToolRail stub buttons. No hover/focus chrome that requires drag. -->
    <Style x:Key="ToolTileStyle" TargetType="Button">
        <Setter Property="Width"             Value="84"/>
        <Setter Property="Height"            Value="56"/>
        <Setter Property="Background"        Value="Transparent"/>
        <Setter Property="BorderBrush"       Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness"   Value="2"/>
        <Setter Property="Foreground"        Value="{StaticResource BrushInk}"/>
        <Setter Property="FontFamily"        Value="{StaticResource FontBody}"/>
        <Setter Property="FontSize"          Value="12"/>
        <Setter Property="FontWeight"        Value="SemiBold"/>
        <Setter Property="Cursor"            Value="Hand"/>
        <Setter Property="FocusVisualStyle"  Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="12,0,0,0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource BrushSurface2}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource BrushAccentSoft}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── TopBar icon button style (36×36 minimum, no border by default) ── -->
    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="MinWidth"          Value="36"/>
        <Setter Property="MinHeight"         Value="36"/>
        <Setter Property="Width"             Value="36"/>
        <Setter Property="Height"            Value="36"/>
        <Setter Property="Background"        Value="Transparent"/>
        <Setter Property="BorderBrush"       Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness"   Value="1.5"/>
        <Setter Property="Foreground"        Value="{StaticResource BrushInk2}"/>
        <Setter Property="Cursor"            Value="Hand"/>
        <Setter Property="FocusVisualStyle"  Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource BrushSurface2}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource BrushAccentSoft}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
```

2. Merge AppStyles.xaml into App.xaml Application.Resources:
```xml
<Application x:Class="MathGaze.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/AppStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
```

3. Create MathGaze/ViewModels/ directory. Create MathGaze/ViewModels/MainViewModel.cs:
```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;

namespace MathGaze.ViewModels;

/// <summary>
/// Top-level ViewModel: file state, mode toggle, zoom, page navigation.
/// Commands for PDF open/close, zoom, page nav, and scroll are wired in Plan 04
/// after PDF service is available. This plan provides the observable state skeleton.
/// </summary>
public partial class MainViewModel : ObservableObject
{
    // ── File state ──────────────────────────────────────────────────────────────
    [ObservableProperty] private string _fileName = string.Empty;
    [ObservableProperty] private bool   _isPdfOpen = false;

    // ── Mode ────────────────────────────────────────────────────────────────────
    /// <summary>True = Practice Mode (angle readouts visible). False = Exam Mode (hidden).</summary>
    [ObservableProperty] private bool _isPracticeMode = true;

    // ── Zoom ────────────────────────────────────────────────────────────────────
    /// <summary>1.0 = 100%. Range: 0.25 to 4.0. Updated by zoom commands in Plan 04.</summary>
    [ObservableProperty] private double _zoomFactor = 1.0;

    /// <summary>Display string for TopBar zoom indicator, e.g. "100%".</summary>
    public string ZoomLabel => $"{(int)Math.Round(ZoomFactor * 100)}%";

    partial void OnZoomFactorChanged(double value) => OnPropertyChanged(nameof(ZoomLabel));

    // ── Page navigation ─────────────────────────────────────────────────────────
    /// <summary>1-based current page index.</summary>
    [ObservableProperty] private int _currentPage = 1;

    [ObservableProperty] private int _totalPages = 0;

    /// <summary>Display string for TopBar page counter, e.g. "7 / 22".</summary>
    public string PageLabel => TotalPages > 0 ? $"{CurrentPage} / {TotalPages}" : "— / —";

    partial void OnCurrentPageChanged(int value) => OnPropertyChanged(nameof(PageLabel));
    partial void OnTotalPagesChanged(int value)  => OnPropertyChanged(nameof(PageLabel));

    // ── Mode toggle (stub — full command wired in Plan 04) ───────────────────────
    [RelayCommand]
    private void ToggleMode() => IsPracticeMode = !IsPracticeMode;
}
```

4. Update App.xaml.cs to register MainViewModel in DI (add to services block):
```csharp
services.AddSingleton<MainViewModel>();
services.AddSingleton<MainWindow>();
```
Add `using MathGaze.ViewModels;` at the top.

5. Run `dotnet build MathGaze.sln` — must exit 0 before proceeding to Task 2.
  </action>
  <verify>
    <automated>dotnet build MathGaze.sln --no-incremental 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build MathGaze.sln` exits 0
    - `MathGaze/Styles/AppStyles.xaml` exists and contains the string `ToolTileStyle`
    - `MathGaze/Styles/AppStyles.xaml` contains the string `BrushAccent`
    - `MathGaze/ViewModels/MainViewModel.cs` contains the string `ObservableProperty`
    - `MathGaze/ViewModels/MainViewModel.cs` contains the string `IsPracticeMode`
    - `MathGaze/ViewModels/MainViewModel.cs` contains the string `ZoomFactor`
    - `MathGaze/ViewModels/MainViewModel.cs` contains the string `PageLabel`
    - `MathGaze/App.xaml` contains the string `AppStyles.xaml`
  </acceptance_criteria>
  <done>Design tokens in AppStyles.xaml, MainViewModel with observable state properties, DI registration updated.</done>
</task>

<task type="auto">
  <name>Task 2: Build 3-column shell — TopBar, ToolRail, PdfCanvas, ScrollRail, RightRail</name>
  <files>
    MathGaze/MainWindow.xaml
    MathGaze/MainWindow.xaml.cs
    MathGaze/Views/TopBar.xaml
    MathGaze/Views/TopBar.xaml.cs
    MathGaze/Views/ToolRail.xaml
    MathGaze/Views/ToolRail.xaml.cs
    MathGaze/Views/PdfCanvas.xaml
    MathGaze/Views/PdfCanvas.xaml.cs
    MathGaze/Views/ScrollRail.xaml
    MathGaze/Views/ScrollRail.xaml.cs
    MathGaze/Views/RightRailPlaceholder.xaml
    MathGaze/Views/RightRailPlaceholder.xaml.cs
  </files>
  <read_first>
    - docs/direction-splitrails.jsx (SplitRails component: top=60, left=T.s(104), right=T.s(148); TopBar layout; ToolRail with tools array; ScrollRail position; RightRailEmpty)
    - docs/shared.jsx (Icon SVG paths for each tool name; ModePill component; design tokens)
    - MathGaze/Styles/AppStyles.xaml (ToolTileStyle, IconButtonStyle, all brush keys)
    - MathGaze/ViewModels/MainViewModel.cs (observable properties to bind to)
  </read_first>
  <action>
Layout reference from direction-splitrails.jsx SplitRails component:
- TopBar: height=60px, position=top, full width, zIndex=3
- ToolRail: left=0, top=60, width=104px (T.s(104)), full height below TopBar, zIndex=2
- Canvas: left=104, top=60, right=148 (right rail width), bottom=0
- ScrollRail: right=148+6px from right of canvas, width=38px, top=60+14, bottom=14
- RightRail: right=0, top=60, width=148px (T.s(148)), full height below TopBar, zIndex=2

TopBar from direction-splitrails.jsx (left to right):
1. Branding pill: 28×28 "M" logo box (BrushAccent background, white text) + "MathGaze" text + down chevron — border #E8E6E0, radius 8
2. File chip: folder icon + filename in mono font + 36×36 Open button (folder icon) + 36×36 Close button (X icon) — BrushSurface2 background, border
3. ModePill: coloured dot (BrushPractice or BrushExam) + "Practice Mode" / "Exam Mode" text
4. Spacer (flex:1)
5. Zoom strip: 32×32 minus button + "100%" label (mono, minWidth 42) + 32×32 plus button + divider + 32×32 fit-page button — border, radius 8
6. Page strip: 36×32 prev button + "— / —" counter (mono) + 36×32 next button — border, radius 8
7. 40×40 settings button (placeholder, no action)

ToolRail tools array (from direction-splitrails.jsx tools array, 6 tools per D-04 — exclude 'highlight' which is MVP but out of scope for Phase 1 stubs):
['cursor'/'Select', 'point'/'Point', 'line'/'Line', 'circle'/'Circle', 'protractor'/'Protractor', 'text'/'Text']

Icon SVG path data (from shared.jsx, converted to WPF Geometry/Path syntax):
- cursor:    "M5 3l6 16 2-7 7-2z"
- point:     circle cx=12 cy=12 r=2.5 (filled) + circle cx=12 cy=12 r=7
- line:      circle cx=5 cy=19 r=1.5 + circle cx=19 cy=5 r=1.5 + "M5 19L19 5"
- circle:    circle cx=12 cy=12 r=8 + circle cx=12 cy=12 r=1 (filled)
- protractor:"M3 16a9 9 0 0 1 18 0" + "M3 16h18" + tick marks + circle cx=12 cy=16 r=1
- text:      "M5 6h14M12 6v13M9 19h6"

ScrollRail from direction-splitrails.jsx ScrollRail component:
- Vertical stack, positioned to the right of canvas area, left of right rail
- Top group: PageUp + Up buttons (30×30 each)
- Middle: scroll track with position indicator (visual only — not interactive in Phase 1)
- Bottom group: Down + PageDown buttons (30×30 each)
Note: ScrollRail buttons are 30×30 in the JSX design. This is BELOW the 56px gaze floor from HANDOFF.md. In XAML, set MinWidth/MinHeight=56 on each scroll button to meet the gaze requirement. The container width stays at 38px per design but buttons expand to 56px height.

RightRailEmpty from direction-splitrails.jsx:
- Dashed border box: "Nothing selected" label (uppercase, 9px, BrushInk3) + "Pick a tool on the left, or click an object on the page." body text

Create all files:

1. Create MathGaze/Views/ directory.

2. Create MathGaze/Views/TopBar.xaml:
```xml
<UserControl x:Class="MathGaze.Views.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="60"
             Background="{StaticResource BrushSurface}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Border BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1">
        <DockPanel Margin="16,0" LastChildFill="False">

            <!-- 1. Branding pill -->
            <Border DockPanel.Dock="Left" VerticalAlignment="Center"
                    BorderBrush="{StaticResource BrushBorder}" BorderThickness="1.5"
                    CornerRadius="8" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal" Margin="6,6,10,6">
                    <Border Width="28" Height="28" CornerRadius="6"
                            Background="{StaticResource BrushAccent}"
                            Margin="0,0,8,0">
                        <TextBlock Text="M" FontSize="14" FontWeight="Black"
                                   Foreground="White" HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="MathGaze" FontSize="14" FontWeight="Bold"
                               Foreground="{StaticResource BrushInk}"
                               VerticalAlignment="Center" Margin="0,0,4,0"/>
                </StackPanel>
            </Border>

            <!-- 2. File chip -->
            <Border DockPanel.Dock="Left" VerticalAlignment="Center"
                    Background="{StaticResource BrushSurface2}"
                    BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                    CornerRadius="8" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal" Margin="12,4,4,4">
                    <!-- File icon 14×14 -->
                    <Viewbox Width="14" Height="14" Margin="0,0,6,0">
                        <Canvas Width="24" Height="24">
                            <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                  StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Data="M7 3h8l5 5v13H7z"/>
                            <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                  Data="M15 3v5h5"/>
                        </Canvas>
                    </Viewbox>
                    <!-- Filename -->
                    <TextBlock x:Name="FileNameText"
                               Text="{Binding FileName, TargetNullValue='No file open'}"
                               FontFamily="{StaticResource FontMono}" FontSize="11"
                               Foreground="{StaticResource BrushInk2}"
                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <!-- Open button -->
                    <Button x:Name="OpenButton" Width="36" Height="36"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Open PDF"
                            Command="{Binding OpenFileCommand}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M3 7a2 2 0 0 1 2-2h4l2 2h8a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                    <!-- Close button -->
                    <Button x:Name="CloseButton" Width="36" Height="36"
                            Style="{StaticResource IconButtonStyle}"
                            ToolTip="Close PDF"
                            Command="{Binding CloseFileCommand}"
                            IsEnabled="{Binding IsPdfOpen}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M6 6l12 12M18 6L6 18"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                </StackPanel>
            </Border>

            <!-- 3. Mode pill -->
            <Border DockPanel.Dock="Left" VerticalAlignment="Center"
                    BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                    CornerRadius="999" Margin="0,0,10,0"
                    Background="Transparent">
                <StackPanel Orientation="Horizontal" Margin="12,6">
                    <!-- Coloured dot — BrushPractice when isPracticeMode, BrushExam otherwise -->
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="{StaticResource BrushExam}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPracticeMode}" Value="True">
                                        <Setter Property="Fill" Value="{StaticResource BrushPractice}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource BrushInk}"
                               LetterSpacing="0.6" TextOptions.TextFormattingMode="Display">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Exam Mode"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPracticeMode}" Value="True">
                                        <Setter Property="Text" Value="Practice Mode"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Settings button (right-docked) -->
            <Button DockPanel.Dock="Right" Width="40" Height="40"
                    Style="{StaticResource IconButtonStyle}"
                    VerticalAlignment="Center" Margin="10,0,0,0"
                    ToolTip="Settings (not yet implemented)">
                <Viewbox Width="18" Height="18">
                    <Canvas Width="24" Height="24">
                        <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Data="M12 2v3M12 19v3M4.2 4.2l2.1 2.1M17.7 17.7l2.1 2.1M2 12h3M19 12h3M4.2 19.8l2.1-2.1M17.7 6.3l2.1-2.1"/>
                        <Ellipse Canvas.Left="9" Canvas.Top="9" Width="6" Height="6"
                                 Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"/>
                    </Canvas>
                </Viewbox>
            </Button>

            <!-- Page navigation strip (right-docked) -->
            <Border DockPanel.Dock="Right" VerticalAlignment="Center"
                    BorderBrush="{StaticResource BrushBorder}" BorderThickness="1.5"
                    CornerRadius="8" Margin="10,0,0,0">
                <StackPanel Orientation="Horizontal" Margin="3">
                    <!-- Prev page -->
                    <Button Width="36" Height="32" Style="{StaticResource IconButtonStyle}"
                            BorderThickness="0" ToolTip="Previous page"
                            Command="{Binding PreviousPageCommand}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M15 6l-6 6 6 6M8 6v12"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                    <!-- Page counter -->
                    <TextBlock Text="{Binding PageLabel}"
                               FontFamily="{StaticResource FontMono}" FontSize="12"
                               Foreground="{StaticResource BrushInk}" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="4,0" MinWidth="52"
                               TextAlignment="Center"/>
                    <!-- Next page -->
                    <Button Width="36" Height="32" Style="{StaticResource IconButtonStyle}"
                            BorderThickness="0" ToolTip="Next page"
                            Command="{Binding NextPageCommand}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M9 6l6 6-6 6M16 6v12"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Zoom strip (right-docked) -->
            <Border DockPanel.Dock="Right" VerticalAlignment="Center"
                    BorderBrush="{StaticResource BrushBorder}" BorderThickness="1.5"
                    CornerRadius="8" Margin="10,0,0,0">
                <StackPanel Orientation="Horizontal" Margin="3">
                    <!-- Zoom out -->
                    <Button Width="32" Height="32" Style="{StaticResource IconButtonStyle}"
                            BorderThickness="0" ToolTip="Zoom out"
                            Command="{Binding ZoomOutCommand}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M5 12h14"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                    <!-- Zoom label -->
                    <TextBlock Text="{Binding ZoomLabel}"
                               FontFamily="{StaticResource FontMono}" FontSize="11"
                               Foreground="{StaticResource BrushInk}" FontWeight="SemiBold"
                               VerticalAlignment="Center" MinWidth="42" TextAlignment="Center"
                               Margin="6,0"/>
                    <!-- Zoom in -->
                    <Button Width="32" Height="32" Style="{StaticResource IconButtonStyle}"
                            BorderThickness="0" ToolTip="Zoom in"
                            Command="{Binding ZoomInCommand}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M12 5v14M5 12h14"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                    <!-- Divider -->
                    <Rectangle Width="1" Height="18" Fill="{StaticResource BrushBorder}" Margin="2,0"/>
                    <!-- Fit page -->
                    <Button Width="32" Height="32" Style="{StaticResource IconButtonStyle}"
                            BorderThickness="0" ToolTip="Fit page"
                            Command="{Binding FitPageCommand}">
                        <Viewbox Width="16" Height="16">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M4 9V5h4M20 9V5h-4M4 15v4h4M20 15v4h-4"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                </StackPanel>
            </Border>

        </DockPanel>
    </Border>
</UserControl>
```

3. Create MathGaze/Views/TopBar.xaml.cs (code-behind, minimal):
```csharp
using System.Windows.Controls;
namespace MathGaze.Views;
public partial class TopBar : UserControl
{
    public TopBar() { InitializeComponent(); }
}
```

4. Create MathGaze/Views/ToolRail.xaml — 6 stub ToolTile buttons per D-04:
```xml
<UserControl x:Class="MathGaze.Views.ToolRail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="104"
             Background="{StaticResource BrushSurface2}">
    <Border BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,1,0">
        <DockPanel>
            <!-- "TOOLS" label -->
            <TextBlock DockPanel.Dock="Top"
                       Text="TOOLS" FontSize="9" FontWeight="Bold"
                       Foreground="{StaticResource BrushAccentInk}"
                       HorizontalAlignment="Center" Margin="0,14,0,8"
                       LetterSpacing="1.2"/>
            <!-- Tool buttons -->
            <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Center" Margin="0,0,0,0">

                <!-- Select -->
                <Button Style="{StaticResource ToolTileStyle}" Margin="0,0,0,4" ToolTip="Select tool (Phase 2)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                            <Canvas Width="24" Height="24">
                                <Path Fill="{StaticResource BrushInk}" Data="M5 3l6 16 2-7 7-2z"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Select" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushInk}"/>
                    </StackPanel>
                </Button>

                <!-- Point -->
                <Button Style="{StaticResource ToolTileStyle}" Margin="0,0,0,4" ToolTip="Point tool (Phase 2)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                            <Canvas Width="24" Height="24">
                                <Ellipse Canvas.Left="9.5" Canvas.Top="9.5" Width="5" Height="5"
                                         Fill="{StaticResource BrushInk}"/>
                                <Ellipse Canvas.Left="5" Canvas.Top="5" Width="14" Height="14"
                                         Stroke="{StaticResource BrushInk}" StrokeThickness="1.8" Fill="Transparent"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Point" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushInk}"/>
                    </StackPanel>
                </Button>

                <!-- Line -->
                <Button Style="{StaticResource ToolTileStyle}" Margin="0,0,0,4" ToolTip="Line tool (Phase 2)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                            <Canvas Width="24" Height="24">
                                <Ellipse Canvas.Left="3.5" Canvas.Top="17.5" Width="3" Height="3"
                                         Fill="{StaticResource BrushInk}"/>
                                <Ellipse Canvas.Left="17.5" Canvas.Top="3.5" Width="3" Height="3"
                                         Fill="{StaticResource BrushInk}"/>
                                <Path Stroke="{StaticResource BrushInk}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M5 19L19 5"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Line" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushInk}"/>
                    </StackPanel>
                </Button>

                <!-- Circle -->
                <Button Style="{StaticResource ToolTileStyle}" Margin="0,0,0,4" ToolTip="Circle tool (Phase 2)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                            <Canvas Width="24" Height="24">
                                <Ellipse Canvas.Left="4" Canvas.Top="4" Width="16" Height="16"
                                         Stroke="{StaticResource BrushInk}" StrokeThickness="1.8" Fill="Transparent"/>
                                <Ellipse Canvas.Left="11" Canvas.Top="11" Width="2" Height="2"
                                         Fill="{StaticResource BrushInk}"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Circle" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushInk}"/>
                    </StackPanel>
                </Button>

                <!-- Protractor -->
                <Button Style="{StaticResource ToolTileStyle}" Margin="0,0,0,4" ToolTip="Protractor tool (Phase 3)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Fill="Transparent"
                                      Data="M3 16a9 9 0 0 1 18 0"/>
                                <Path Stroke="{StaticResource BrushInk}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M3 16h18"/>
                                <Path Stroke="{StaticResource BrushInk}" StrokeThickness="0.8"
                                      Data="M7 16v-1.5M10 16v-2M14 16v-2M17 16v-1.5"/>
                                <Ellipse Canvas.Left="11" Canvas.Top="15" Width="2" Height="2"
                                         Fill="{StaticResource BrushInk}"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Protractor" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushInk}" FontSize="11"/>
                    </StackPanel>
                </Button>

                <!-- Text -->
                <Button Style="{StaticResource ToolTileStyle}" Margin="0,0,0,4" ToolTip="Text tool (Phase 4)">
                    <StackPanel Orientation="Horizontal">
                        <Viewbox Width="24" Height="24" Margin="0,0,10,0">
                            <Canvas Width="24" Height="24">
                                <Path Stroke="{StaticResource BrushInk}" StrokeThickness="1.8"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M5 6h14M12 6v13M9 19h6"/>
                            </Canvas>
                        </Viewbox>
                        <TextBlock Text="Text" VerticalAlignment="Center"
                                   Foreground="{StaticResource BrushInk}"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
```

5. Create MathGaze/Views/ToolRail.xaml.cs:
```csharp
using System.Windows.Controls;
namespace MathGaze.Views;
public partial class ToolRail : UserControl
{
    public ToolRail() { InitializeComponent(); }
}
```

6. Create MathGaze/Views/PdfCanvas.xaml (SKXamlCanvas placeholder — full implementation in Plan 03):
```xml
<UserControl x:Class="MathGaze.Views.PdfCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             Background="{StaticResource BrushBg}">
    <Grid>
        <skia:SKXamlCanvas x:Name="SkCanvas"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           PaintSurface="OnPaintSurface"
                           IgnorePixelScaling="False"/>
    </Grid>
</UserControl>
```

7. Create MathGaze/Views/PdfCanvas.xaml.cs with minimal PaintSurface that draws a light grey background:
```csharp
using SkiaSharp;
using SkiaSharp.Views.WPF;
using System.Windows.Controls;

namespace MathGaze.Views;

public partial class PdfCanvas : UserControl
{
    public PdfCanvas()
    {
        InitializeComponent();
    }

    private void OnPaintSurface(object sender, SKPaintSurfaceEventArgs e)
    {
        var canvas = e.Surface.Canvas;
        canvas.Clear(new SKColor(0xF5, 0xF3, 0xEE)); // BrushBg colour
        canvas.Flush();
    }
}
```

8. Create MathGaze/Views/ScrollRail.xaml:
```xml
<UserControl x:Class="MathGaze.Views.ScrollRail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="38">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top: PageUp + Up -->
        <Border Grid.Row="0" Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                CornerRadius="10" Padding="4" Margin="0,14,0,4">
            <StackPanel HorizontalAlignment="Center">
                <!-- Page Up — 30×56 minimum gaze target -->
                <Button Width="30" Height="56" Style="{StaticResource IconButtonStyle}"
                        BorderThickness="0" ToolTip="Page Up"
                        Command="{Binding ScrollPageUpCommand}">
                    <Viewbox Width="16" Height="16">
                        <Canvas Width="24" Height="24">
                            <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Data="M6 14l6-6 6 6M6 18h12"/>
                        </Canvas>
                    </Viewbox>
                </Button>
                <!-- Up -->
                <Button Width="30" Height="56" Style="{StaticResource IconButtonStyle}"
                        BorderThickness="0" ToolTip="Scroll Up"
                        Command="{Binding ScrollUpCommand}">
                    <Viewbox Width="16" Height="16">
                        <Canvas Width="24" Height="24">
                            <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Data="M6 15l6-6 6 6"/>
                        </Canvas>
                    </Viewbox>
                </Button>
            </StackPanel>
        </Border>

        <!-- Middle: scroll position indicator (visual only) -->
        <Border Grid.Row="1" Background="{StaticResource BrushSurface2}"
                BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                CornerRadius="999" Margin="4">
            <Grid>
                <Rectangle Fill="{StaticResource BrushAccent}" Opacity="0.85"
                           RadiusX="999" RadiusY="999"
                           Width="10" Height="20"
                           VerticalAlignment="Top" Margin="0,10,0,0"/>
            </Grid>
        </Border>

        <!-- Bottom: Down + PageDown -->
        <Border Grid.Row="2" Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                CornerRadius="10" Padding="4" Margin="0,4,0,14">
            <StackPanel HorizontalAlignment="Center">
                <!-- Down -->
                <Button Width="30" Height="56" Style="{StaticResource IconButtonStyle}"
                        BorderThickness="0" ToolTip="Scroll Down"
                        Command="{Binding ScrollDownCommand}">
                    <Viewbox Width="16" Height="16">
                        <Canvas Width="24" Height="24">
                            <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Data="M6 9l6 6 6-6"/>
                        </Canvas>
                    </Viewbox>
                </Button>
                <!-- Page Down -->
                <Button Width="30" Height="56" Style="{StaticResource IconButtonStyle}"
                        BorderThickness="0" ToolTip="Page Down"
                        Command="{Binding ScrollPageDownCommand}">
                    <Viewbox Width="16" Height="16">
                        <Canvas Width="24" Height="24">
                            <Path Stroke="{StaticResource BrushInk2}" StrokeThickness="1.8"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  Data="M6 6h12M6 10l6 6 6-6"/>
                        </Canvas>
                    </Viewbox>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
```

9. Create MathGaze/Views/ScrollRail.xaml.cs:
```csharp
using System.Windows.Controls;
namespace MathGaze.Views;
public partial class ScrollRail : UserControl
{
    public ScrollRail() { InitializeComponent(); }
}
```

10. Create MathGaze/Views/RightRailPlaceholder.xaml:
```xml
<UserControl x:Class="MathGaze.Views.RightRailPlaceholder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="148"
             Background="{StaticResource BrushSurface}">
    <Border BorderBrush="{StaticResource BrushBorder}" BorderThickness="1,0,0,0">
        <StackPanel Margin="12,14">
            <!-- Dashed "Nothing selected" box -->
            <Border BorderBrush="{StaticResource BrushBorder}" BorderThickness="1.5"
                    CornerRadius="10" Padding="12,14">
                <Border.BorderDashArray>
                    <DoubleCollection>4,4</DoubleCollection>
                </Border.BorderDashArray>
                <StackPanel>
                    <TextBlock Text="NOTHING SELECTED"
                               FontSize="9" FontWeight="Bold"
                               Foreground="{StaticResource BrushInk3}"
                               LetterSpacing="1.2" Margin="0,0,0,4"/>
                    <TextBlock Text="Pick a tool on the left, or click an object on the page."
                               FontSize="12" Foreground="{StaticResource BrushInk2}"
                               TextWrapping="Wrap" LineHeight="17"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
```

Note: WPF Border does not natively support dashed borders. Use a workaround: create an outer Border with CornerRadius and use a Rectangle with StrokeDashArray inside, OR use the approach of layering. Simplest working approach: replace the dashed Border with a Rectangle using a custom Style. If dashed border proves complex, use a simple dotted pattern with a custom ControlTemplate. An acceptable fallback is a solid thin border (1.5px BrushBorder) with the same content — the "dashed" visual is a nice-to-have, not a functional requirement.

11. Create MathGaze/Views/RightRailPlaceholder.xaml.cs:
```csharp
using System.Windows.Controls;
namespace MathGaze.Views;
public partial class RightRailPlaceholder : UserControl
{
    public RightRailPlaceholder() { InitializeComponent(); }
}
```

12. Replace MainWindow.xaml with the full 3-column shell:
```xml
<Window x:Class="MathGaze.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:MathGaze.Views"
        xmlns:vm="clr-namespace:MathGaze.ViewModels"
        Title="MathGaze" Height="800" Width="1280"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BrushBg}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>   <!-- TopBar -->
            <RowDefinition Height="*"/>    <!-- Content area -->
        </Grid.RowDefinitions>

        <!-- TopBar — spans full width, row 0 -->
        <views:TopBar Grid.Row="0"
                      DataContext="{Binding}"
                      Panel.ZIndex="3"/>

        <!-- Content area — row 1 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="104"/>  <!-- ToolRail -->
                <ColumnDefinition Width="*"/>    <!-- Canvas + ScrollRail -->
                <ColumnDefinition Width="148"/>  <!-- RightRail -->
            </Grid.ColumnDefinitions>

            <!-- Left tool rail -->
            <views:ToolRail Grid.Column="0" Panel.ZIndex="2"/>

            <!-- Centre: canvas -->
            <views:PdfCanvas Grid.Column="1" x:Name="PdfCanvasView"
                             DataContext="{Binding}"/>

            <!-- ScrollRail overlaid on right edge of canvas, inside canvas column -->
            <views:ScrollRail Grid.Column="1"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Stretch"
                              DataContext="{Binding}"
                              Margin="0,0,6,0"
                              Panel.ZIndex="2"/>

            <!-- Right rail -->
            <views:RightRailPlaceholder Grid.Column="2" Panel.ZIndex="2"/>
        </Grid>
    </Grid>
</Window>
```

13. Replace MainWindow.xaml.cs to inject MainViewModel via DI constructor:
```csharp
using MathGaze.ViewModels;
using System.Windows;

namespace MathGaze;

public partial class MainWindow : Window
{
    public MainWindow(MainViewModel viewModel)
    {
        InitializeComponent();
        DataContext = viewModel;
    }
}
```

14. Run `dotnet build MathGaze.sln` — must exit 0. Then run the app (`dotnet run --project MathGaze`) to visually confirm the 3-column layout appears.
  </action>
  <verify>
    <automated>dotnet build MathGaze.sln --no-incremental 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build MathGaze.sln` exits 0
    - `MathGaze/Views/TopBar.xaml` exists and contains the string `IsPracticeMode`
    - `MathGaze/Views/ToolRail.xaml` exists and contains the string `ToolTileStyle` (used 6 times)
    - `MathGaze/Views/PdfCanvas.xaml` exists and contains the string `SKXamlCanvas`
    - `MathGaze/Views/ScrollRail.xaml` exists and contains the string `ScrollPageUpCommand`
    - `MathGaze/Views/RightRailPlaceholder.xaml` exists and contains the string `NOTHING SELECTED`
    - `MathGaze/MainWindow.xaml` contains the string `Width="104"` (ToolRail column) and `Width="148"` (RightRail column)
    - `MathGaze/MainWindow.xaml.cs` contains the string `MainViewModel viewModel` (DI constructor injection)
  </acceptance_criteria>
  <done>3-column WPF shell is running: TopBar with mode pill, 6 stub ToolTile buttons in left rail, SkiaSharp canvas in centre, ScrollRail overlay, RightRail placeholder. Build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual checkpoint — confirm 3-column shell matches design intent</name>
  <what-built>
    3-column WPF shell built from direction-splitrails.jsx design reference:
    - TopBar (height 60px): MathGaze branding pill, file chip with Open/Close buttons, Practice Mode pill, zoom strip, page nav strip, settings button
    - Left ToolRail (width 104px): 6 ToolTile stubs — Select, Point, Line, Circle, Protractor, Text
    - Centre: SkiaSharp canvas (light grey #F5F3EE background, no PDF yet)
    - ScrollRail: overlaid on right edge of canvas (PageUp, Up, Down, PageDown buttons)
    - Right RightRailPlaceholder (width 148px): "NOTHING SELECTED" box
  </what-built>
  <how-to-verify>
    Run the app: `dotnet run --project MathGaze/MathGaze.csproj`

    Check each of the following:
    1. Window launches with title "MathGaze" — no crash on startup
    2. TopBar is visible across the full width with a bottom border line
    3. The "M" logo box (cobalt blue square) and "MathGaze" text are visible in the top-left
    4. The mode pill reads "Practice Mode" with a green dot
    5. Zoom strip shows "100%" with minus/plus/fit-page buttons
    6. Page counter shows "— / —" with prev/next buttons
    7. Left rail shows 6 buttons labelled Select, Point, Line, Circle, Protractor, Text
    8. Each ToolTile button is visually large (≥56px tall, ≥84px wide) — you should be able to click them easily
    9. Canvas area is a flat light cream/grey background colour (no PDF yet — that's Plan 03)
    10. Right rail is visible with a "NOTHING SELECTED" placeholder
    11. No flyout menus, no popup windows, no dialog boxes appear on launch
  </how-to-verify>
  <resume-signal>Type "approved" if the layout matches the above checklist, or describe specific issues (e.g. "TopBar missing", "buttons too small", "crash on startup")</resume-signal>
</task>

</tasks>

<threat_model>
  <threat id="T-02-01" likelihood="low" impact="low">
    <description>Spoofing: the Open button in the TopBar is wired to OpenFileCommand in MainViewModel. In this plan, OpenFileCommand does not yet exist — the Button Command binding will silently fail (no exception, button just does nothing). This is expected and safe because no file loading path exists yet.</description>
    <mitigation>Stub commands (OpenFileCommand, CloseFileCommand, ZoomInCommand, ZoomOutCommand, FitPageCommand, PreviousPageCommand, NextPageCommand, scroll commands) are declared as [RelayCommand] in MainViewModel with empty method bodies in Plan 04. XAML binding errors are non-fatal in WPF but will appear in debug output — acceptable for this plan.</mitigation>
  </threat>
  <threat id="T-02-02" likelihood="low" impact="low">
    <description>Information Disclosure: design token colours are hardcoded in AppStyles.xaml. If the file is decompiled from the published EXE, colour values are visible. No sensitive data is embedded.</description>
    <mitigation>Accept. Colour values are UI presentation data, not secrets. No mitigation needed.</mitigation>
  </threat>
</threat_model>

<verification>
Final check after all tasks:
1. `dotnet build MathGaze.sln` exits 0
2. `dotnet test MathGaze.Tests` still passes 32 tests (regression check)
3. App launches to 3-column shell (verified by human checkpoint)
4. All 6 ToolTile buttons are visible and at least 84×56 WPF logical units
5. Mode pill is visible in TopBar at all times (per D-05 and HANDOFF.md requirement)
</verification>

<success_criteria>
- 3-column shell is running and visually correct per direction-splitrails.jsx design reference
- TopBar is fully functional in appearance (visual + data binding) per D-05
- 6 stub ToolTile buttons present in left rail per D-03, D-04
- All gaze targets >= 56px as required by HANDOFF.md
- No flyouts, no popups — all UI in-window panels per Grid 3 compatibility rules
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
